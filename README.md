# PassIntel-AI
PassIntel AI is an offline-capable, client-side PWA built with React and TypeScript. It profiles password vulnerabilities character-by-character using an N-gram language model heatmap, calculates Shannon entropy, and uses an AI-powered mutation engine to harden weak passwords into secure, memorable variants.
